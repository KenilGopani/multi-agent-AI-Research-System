SEARCH_QUERY_PROMPT = """You are a research specialist.

Given the query: {query}

Generate exactly 3 different optimized web search queries for finding relevant, recent, and credible sources.
Return only the queries, one per line, without numbering or commentary."""
